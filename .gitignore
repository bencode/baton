node_modules
dist
*.tsbuildinfo
.DS_Store
*.log

# Local env (secrets, model overrides) — never commit
.env
.env.local
.env.*.local

# Prisma local dev database (SQLite)
*.db
*.db-journal
*.sqlite

# Bun lockfile (this repo is pnpm-managed)
bun.lockb

# Claude Code runtime artifacts (per-session lockfile)
.claude/scheduled_tasks.lock

# baton local state (per-checkout: server URL, worker identity, session tokens)
.baton.json

# Local drafts (design notes / plans) — kept out of the repo
docs/
